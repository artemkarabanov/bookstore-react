import { BurgerMenu } from "components/BurgerMenu";
import { StyledHeader } from "./style";

export const Header = () => {
  return (
    <StyledHeader>
      <BurgerMenu />
    </StyledHeader>
  );
};
